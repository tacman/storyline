## Demo pages

- `demo/static/index.html` - no-build ESM demo that reads `storyline.json`

Start a local server from the repository root:

```bash
php -S 127.0.0.1:8000 -t .
```

Then open:

- `http://127.0.0.1:8000/demo/static/index.html`
